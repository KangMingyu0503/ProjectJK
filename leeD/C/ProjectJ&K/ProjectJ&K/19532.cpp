#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>

int main() {
	int arr[6];
	int x, y;

	for (int i = 0; i < 6; i++) {
		scanf("%d", &arr[i]);
	}
	//if (arr[0] > 0 && arr[3] > 0)
	
}